import { SortStringPipe } from './sort-string.pipe';

describe('SortStringPipe', () => {
  it('create an instance', () => {
    const pipe = new SortStringPipe();
    expect(pipe).toBeTruthy();
  });
});
